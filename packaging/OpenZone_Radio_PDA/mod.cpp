// Companion mod: the radio as a board in an OpenZone PDA bay.
//
// Its dependencies are HARD (see the pbo's requiredAddons): a server without
// OpenZone Core and OpenZone PDA gets a blocking window before the game
// loads, not a silent skip. The description says so, because the alternative
// is a server owner discovering it at start-up.

name        = "OpenZone Radio PDA";
author      = "Zone Protocol";
authorID    = "";
version     = "0.1";
description = "Companion mod. Puts OpenZone Radio into a PDA device bay as a board. REQUIRES OpenZone Radio, OpenZone Core and OpenZone PDA -- without all three the game refuses to load. github.com/covalschi/openzone-radio";
tooltip     = "OpenZone Radio PDA";
overview    = "The radio as a board in an OpenZone PDA bay. Install only alongside OpenZone Radio, OpenZone Core and OpenZone PDA.";
